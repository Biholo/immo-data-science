pandas>=2.0
scikit-learn>=1.4
joblib>=1.3
scipy>=1.10
openpyxl>=3.1
lightgbm>=4.0

# Optional — price_model.py degrades gracefully (prints a warning, skips
# shap_summary.csv) if not installed. Needed for the SHAP values mentioned
# in CONTEXTE_ML_MEMOIRE.md §3 "Résultats à conserver".
shap>=0.45
